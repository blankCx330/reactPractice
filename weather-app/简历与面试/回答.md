# 关于深化顺序的疑问回答

## 你的疑问

> 指导书里优先推荐我深化使用 query，但我实际修改中的体验，反而觉得应该先将代码检测和 zustand 安装，这是对的吗？

---

## 我的回答

**你的感受是对的。** 让我详细解释：

---

### 文档的实际推荐顺序

| 阶段 | 内容 | 文档定位 |
|-----|------|---------|
| 阶段0 | 代码清理 | 30分钟，清理代码问题 |
| 阶段1 | React Query 深化 | P0 最高优先级，"面试必问" |
| 阶段2 | Zustand + 收藏 | P1 重要改进 |

文档在"深化优先级"部分（2.1节）确实把 **React Query 深化** 放在了最显眼的位置，标注为"面试必问 ⭐⭐⭐⭐⭐"。这容易让人觉得应该先做 Query 深化。

---

### 为什么你的体验是对的

#### 1. 代码清理应该最先做

阶段0的任务是：
- 删除 console.log
- 修复 any 类型
- 添加列表 key
- 运行 lint 检查

这些是**基础设施清理**。如果代码有很多问题，后续深化会更困难：
- 有 any 类型 → 类型推断不准确 → 深化时容易引入新bug
- 有 console.log → 不专业，且可能影响调试
- lint 有警告 → 改代码时更难发现问题

**结论：代码清理是"打扫战场"，应该在"建设新功能"之前完成。**

#### 2. Zustand 安装与 React Query 深化是独立的

这两个任务**没有依赖关系**：

| React Query 深化 | Zustand 安装 |
|-----------------|-------------|
| 优化现有代码 | 添加新功能 |
| 零新增依赖 | 需要安装 zustand |
| 涉及 Loading/Error 处理 | 涉及状态管理 |
| 修改 WeatherApp.tsx、TopContainer.tsx | 创建新文件 store/useAppStore.ts |

**你完全可以先安装 Zustand，再做 Query 深化，或者反过来，不影响结果。**

#### 3. 实际开发体验更重要

文档是从"面试价值"角度排序的：
- React Query 深化 → 面试必问，性价比高
- Zustand → 展示状态管理能力

但你的体验是从"开发体验"角度出发的：
- 先清理代码 → 后续工作更顺畅
- 先安装 Zustand → 可以边做边测试

**两种角度都是合理的。** 文档强调面试价值，你强调开发效率，没有谁对谁错。

---

### 推荐的实际执行顺序

基于你的体验，我建议：

```
最优顺序：
├── 1. 代码清理（阶段0）────── 打扫战场，必须先做
├── 2. 代码检测（npm run lint + build）────── 确认环境干净
├── 3. 安装 Zustand ────────── 独立功能，随时可做
├── 4. React Query 深化 ────── 优化现有代码
├── 5. ECharts 可视化 ──────── 添加视觉效果
└── 6. PWA + 测试 ──────────── 最后的加分项
```

---

### 总结

| 你的感受 | 文档推荐 | 结论 |
|---------|---------|------|
| 先代码检测再深化 | 先 React Query 深化 | **都对**，角度不同 |
| 先安装 Zustand | React Query 深化优先级更高 | **都对**，无依赖关系 |

**你的体验非常有价值**，说明你在实际操作中思考了"什么顺序效率最高"。文档是参考，不是教条。实际开发中，先清理代码、确认环境正常，再深化功能，是更稳妥的做法。

---

> **建议**：按照你的节奏来。先完成代码清理和检测，确认项目没有基础问题后，再根据心情选择先做 Query 深化还是 Zustand 安装。两者都是加分项，做哪个都行。

---

**创建日期**：2026-03-14

---

# 关于是否加入 ESLint + Prettier 的建议

## 你的疑问

> 我是否应该在我的项目中先加入 ESLint + Prettier？

---

## 我的回答

### 项目现状检查

经过检查，你的项目：

| 工具 | 状态 | 配置情况 |
|-----|------|---------|
| **ESLint** | ✅ 已配置 | `eslint.config.js`（flat config 格式） |
| **Prettier** | ❌ 未配置 | 无 `.prettierrc*` 文件 |

**ESLint 已有的规则：**
- TypeScript ESLint（类型检查规则）
- React Hooks 规则（hooks 使用规范）
- React Refresh 规则（Fast Refresh 兼容性）

---

### 直接回答

**ESLint 不需要加，已经有了。**

**Prettier 建议加，但优先级不高。**

---

### 详细分析

#### 1. ESLint 现状评估

你的 ESLint 配置已经相当完善：

```javascript
// eslint.config.js 已包含
extends: [
  js.configs.recommended,      // JS 最佳实践
  tseslint.configs.recommended, // TypeScript 规则
  reactHooks.configs.flat.recommended, // Hooks 规则
  reactRefresh.configs.vite,   // Vite 兼容性
]
```

这是一个**生产级别的配置**，覆盖了：
- 变量未使用、未定义
- TypeScript 类型错误
- React Hooks 依赖数组问题
- Fast Refresh 兼容性警告

**结论**：ESLint 配置够用，无需改动。

---

#### 2. Prettier 是否需要？

| 考量 | 结论 |
|-----|------|
| **必要性** | 低 - 项目规模小，手动格式化也能保持一致 |
| **面试价值** | 中 - 展示工程化意识 |
| **开发体验** | 高 - 自动格式化，减少代码审查时的格式争论 |
| **投入时间** | 10-15 分钟 |

##### 加 Prettier 的好处：
1. **代码风格统一** - 团队协作或自己回头看代码时，格式一致
2. **减少认知负担** - 不用手动调整格式，保存时自动格式化
3. **与 ESLint 配合** - 解决 ESLint 不管的格式问题（缩进、引号、分号等）

##### 不加的影响：
- 也没什么大问题，就是代码风格可能不一致
- 手动格式化也能工作

---

#### 3. 推荐做法

##### 方案 A：加 Prettier（推荐）

**时机**：在"代码清理"阶段之后，深化功能之前

**步骤**（约 10 分钟）：
```bash
# 1. 安装
npm install -D prettier eslint-config-prettier

# 2. 创建配置
# .prettierrc
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}

# 3. 添加脚本到 package.json
"format": "prettier --write \"src/**/*.{ts,tsx,css}\""

# 4. 安装 VS Code 插件 + 配置保存时格式化
```

##### 方案 B：暂不加

如果时间紧张，可以跳过。Prettier 属于"锦上添花"，不是"雪中送炭"。

---

### 优先级排序

结合你之前的疑问，更新后的推荐顺序：

```
优先级排序（按开发效率）：
├── P0: 代码清理（删除 console.log、修复 any、添加 key）
├── P1: 运行 lint + build，确认无错误
├── P2: 安装 Prettier（可选，10 分钟）────────── 新增
├── P3: Zustand 安装 / React Query 深化（二选一或都做）
└── P4: ECharts 可视化 / PWA / 测试
```

---

### 总结

| 问题 | 答案 |
|-----|------|
| ESLint 需要加吗？ | **不需要**，已有完善配置 |
| Prettier 需要加吗？ | **建议加**，优先级不高但价值明确 |
| 什么时候加？ | 代码清理后、深化功能前，只需 10 分钟 |

> **一句话建议**：ESLint 跳过，Prettier 可加可不加。如果想展示工程化能力，花 10 分钟加上；如果时间紧张，直接进入功能深化也行。

---

# 什么是 ESLint？是 Vite 自带的吗？

## 你的疑问

> 什么是 ESLint？这个是我 Vite React 项目自带的吗？

---

## 我的回答

### 一句话回答

**ESLint 是代码质量检查工具，你的项目配置是 Vite 官方模板自带的。**

---

### 什么是 ESLint？

#### 简单理解

ESLint 就像一个**代码审查助手**，在你写代码时帮你发现问题：

```
你写的代码 → ESLint 检查 → 报告问题（错误/警告）
```

#### 它能检查什么？

| 类别 | 示例问题 | 严重程度 |
|-----|---------|---------|
| **语法错误** | 变量未定义、未使用 | ❌ 错误 |
| **最佳实践** | 使用 `var` 而非 `let/const` | ⚠️ 警告 |
| **React 规范** | Hooks 在条件语句中调用 | ❌ 错误 |
| **类型问题** | TypeScript 类型不匹配 | ❌ 错误 |
| **代码风格** | 缺少分号（需配置） | ⚠️ 警告 |

#### 实际例子

```tsx
// ❌ ESLint 会报错：'unused' is assigned a value but never used
const unused = 123;

// ❌ ESLint 会报错：React Hook "useState" is called conditionally
if (condition) {
  const [state, setState] = useState(0);
}

// ✅ ESLint 通过
const [state, setState] = useState(0);
```

#### 为什么重要？

| 好处 | 说明 |
|-----|------|
| **提前发现 bug** | 运行前就发现问题，而不是上线后崩 |
| **统一代码风格** | 团队协作时，代码看起来像一个人写的 |
| **学习最佳实践** | ESLint 报错会教你正确写法 |
| **面试加分** | 大厂必备，体现工程化意识 |

---

### 是 Vite 自带的吗？

#### 答案：是的

你的 ESLint 配置来自 **Vite 官方 React-TS 模板**。

#### 证据

1. **配置文件格式** - `eslint.config.js` 使用的是 ESLint 9 的 **flat config** 格式，这是 Vite 最新模板采用的配置方式

2. **依赖版本** - 你的 `package.json` 中：
   ```json
   "eslint": "^9.39.1",
   "typescript-eslint": "^8.46.4",
   "eslint-plugin-react-hooks": "^7.0.1",
   "eslint-plugin-react-refresh": "^0.4.24"
   ```
   这些都是 Vite 模板默认包含的

3. **创建命令** - 你应该是这样创建项目的：
   ```bash
   npm create vite@latest weather -- --template react-ts
   ```
   这个命令会自动生成 ESLint 配置

#### Vite 模板默认包含什么？

| 包含项 | 用途 |
|-------|------|
| `eslint` | 核心 lint 引擎 |
| `typescript-eslint` | TypeScript 语法解析 + 规则 |
| `eslint-plugin-react-hooks` | React Hooks 规则检查 |
| `eslint-plugin-react-refresh` | Fast Refresh 兼容性检查 |

---

### 你不需要自己配置

Vite 已经帮你做好了：

```
Vite 模板自带：
├── eslint.config.js ─────── 配置文件（flat config 格式）
├── TypeScript 规则 ───────── 检查类型错误
├── React Hooks 规则 ──────── 检查 hooks 用法
└── React Refresh 规则 ────── 确保 Fast Refresh 正常工作
```

#### 运行方式

```bash
# 检查代码
npm run lint

# 检查并自动修复（如果有可修复的问题）
npm run lint -- --fix
```

---

### ESLint vs TypeScript vs Prettier

很多人容易混淆这三个工具：

| 工具 | 职责 | 检查什么 |
|-----|------|---------|
| **TypeScript** | 类型检查 | 变量类型、函数签名、接口匹配 |
| **ESLint** | 代码质量 | 最佳实践、潜在 bug、React 规范 |
| **Prettier** | 代码格式 | 缩进、引号、分号、换行 |

```
代码 ─→ TypeScript 编译 ─→ 类型错误？
   └── ESLint 检查 ───────→ 代码质量问题？
   └── Prettier 格式化 ───→ 格式不一致？
```

**三者互补，不冲突。**

---

### 总结

| 问题 | 答案 |
|-----|------|
| ESLint 是什么？ | 代码质量检查工具，帮你发现 bug 和不规范代码 |
| 是 Vite 自带的吗？ | **是**，Vite React-TS 模板默认包含 |
| 需要自己配置吗？ | **不需要**，开箱即用 |
| 怎么用？ | `npm run lint` 检查，`npm run lint -- --fix` 自动修复 |

> **一句话**：ESLint 是 Vite 送你的"代码审查助手"，开箱即用，不用折腾。

---

# 写简历前：ESLint 面试考点清单

## 你的疑问

> 如果我想将这个技术栈写到实习简历上，我应该知道些什么？

---

## 我的回答

### 核心原则

**写在简历上的东西，面试官一定会问。**

如果你只用了 Vite 自带的默认配置，面试官问"你配置过 ESLint 吗？"你怎么回答？

**正确姿势**：了解原理 + 知道怎么用 + 能回答扩展问题。

---

### 面试考点分级

#### ⭐⭐⭐ 必须掌握（写简历前必须会）

| 考点 | 问题示例 | 你的项目中的答案 |
|-----|---------|----------------|
| **ESLint 是什么？** | "介绍一下 ESLint" | 代码质量检查工具，静态分析代码发现问题 |
| **解决了什么问题？** | "为什么要用 ESLint？" | 统一代码风格、提前发现 bug、执行最佳实践 |
| **怎么用？** | "你们项目怎么跑 ESLint？" | `npm run lint` 检查，`npm run lint -- --fix` 自动修复 |
| **配置文件在哪？** | "ESLint 配置在哪？" | `eslint.config.js`，使用 ESLint 9 的 flat config 格式 |
| **检查了什么规则？** | "你们项目配置了哪些规则？" | TypeScript 规则、React Hooks 规则、React Refresh 规则 |

#### ⭐⭐ 加分项（能回答更好）

| 考点 | 问题示例 | 答案要点 |
|-----|---------|---------|
| **规则等级** | "ESLint 规则有几种等级？" | `off`(0)、`warn`(1)、`error`(2) |
| **和 TypeScript 区别** | "TypeScript 已经有检查了，为什么还要 ESLint？" | TS 查类型，ESLint 查逻辑和风格，职责不同 |
| **和 Prettier 区别** | "ESLint 和 Prettier 有什么区别？" | ESLint 查质量，Prettier 查格式，互补关系 |
| **CI 集成** | "ESLint 怎么集成到 CI？" | 在 GitHub Actions 中运行 `npm run lint`，不通过则阻止合并 |

#### ⭐ 进阶项（大厂可能问）

| 考点 | 问题示例 | 答案要点 |
|-----|---------|---------|
| **自定义规则** | "写过自定义 ESLint 规则吗？" | 可以用 AST 编写，项目中没用到，但了解原理 |
| **禁用规则** | "怎么临时禁用某行代码的规则？" | `// eslint-disable-next-line rule-name` |
| **flat config** | "知道 flat config 吗？" | ESLint 9 新格式，用 JS 数组替代 JSON 配置，更灵活 |

---

### 你的项目实际配置解析

面试时可能会被问到："你项目里 ESLint 配置了什么？"

#### 配置文件结构

```javascript
// eslint.config.js
export default defineConfig([
  globalIgnores(['dist']),           // 忽略 dist 目录
  {
    files: ['**/*.{ts,tsx}'],        // 检查所有 ts/tsx 文件
    extends: [
      js.configs.recommended,              // JS 基础规则
      tseslint.configs.recommended,        // TypeScript 规则
      reactHooks.configs.flat.recommended, // React Hooks 规则
      reactRefresh.configs.vite,           // Fast Refresh 兼容性
    ],
  },
])
```

#### 各插件作用

| 插件 | 检查什么 | 典型错误示例 |
|-----|---------|-------------|
| `js.configs.recommended` | JS 基础规范 | 未使用的变量、未定义的变量 |
| `tseslint.configs.recommended` | TypeScript 规范 | 隐式 any、类型不匹配 |
| `react-hooks` | Hooks 使用规范 | 条件中调用 Hook、依赖数组缺失 |
| `react-refresh` | Fast Refresh 兼容 | 导出非组件、副作用位置错误 |

---

### 模拟面试问答

#### Q1：介绍一下你们项目用的 ESLint

**参考回答**：

> 我们项目用的是 ESLint 9，配置了 TypeScript 和 React 的规则。主要检查三个方面：
> 1. TypeScript 类型问题，比如隐式 any
> 2. React Hooks 使用规范，比如 Hooks 不能在条件语句中调用
> 3. React Refresh 兼容性，确保 Fast Refresh 正常工作
>
> 我们用 flat config 格式配置，比传统的 `.eslintrc` 更灵活。开发时通过 `npm run lint` 检查，也会在 CI 中自动运行。

#### Q2：TypeScript 已经能检查类型错误了，为什么还要 ESLint？

**参考回答**：

> 它们职责不同：
> - TypeScript 检查**类型系统**的问题，比如变量类型不匹配、接口实现不完整
> - ESLint 检查**代码质量和风格**，比如未使用的变量、React Hooks 的正确使用
>
> 举个例子，TypeScript 不会告诉你"这个变量定义了但没用"，但 ESLint 会。TypeScript 不会检查 React Hooks 的依赖数组是否完整，但 `eslint-plugin-react-hooks` 会。
>
> 两者互补，一起使用能覆盖更全面的问题。

#### Q3：ESLint 规则有几种严重程度？

**参考回答**：

> 三种：
> - `off` 或 `0` - 关闭规则
> - `warn` 或 `1` - 警告，不会阻止构建
> - `error` 或 `2` - 错误，会导致构建失败
>
> 我们项目用的是 `recommended` 预设，已经预设了合理的严重程度。

#### Q4：如果某行代码确实需要违反规则，怎么办？

**参考回答**：

> 可以临时禁用规则：
> ```tsx
> // eslint-disable-next-line no-console
> console.log('debug info')
> ```
> 或者整个文件禁用：
> ```tsx
> /* eslint-disable no-console */
> ```
> 但应该少用，只在确实需要的时候用。

---

### 简历怎么写

#### ❌ 错误写法

```
技术栈：ESLint
```

太简单，面试官不知道你懂多少。

#### ✅ 推荐写法

```
工程化：
- 使用 ESLint 9 + TypeScript ESLint + React Hooks 插件，统一代码规范
- 配置 CI 自动检查，确保代码质量
- （如果加了 Prettier）配合 Prettier 实现代码自动格式化
```

#### 项目经历中提及

```
天气咨询应用（React + TypeScript + Vite）
- 采用 ESLint + TypeScript 严格模式，提前发现类型错误和潜在 bug
- 使用 React Hooks ESLint 插件，确保 Hooks 正确使用
```

---

### 写简历前的自检清单

在把 ESLint 写到简历之前，确认你能回答以下问题：

| # | 问题 | 能回答？ |
|---|------|---------|
| 1 | ESLint 是什么？解决什么问题？ | ☐ |
| 2 | ESLint 和 TypeScript 的区别？ | ☐ |
| 3 | 你们项目配置了哪些规则？ | ☐ |
| 4 | 怎么运行 ESLint？ | ☐ |
| 5 | 规则的严重程度有几种？ | ☐ |
| 6 | 怎么临时禁用某条规则？ | ☐ |

**全部能回答 → 可以写在简历上**

**有不会的 → 先补课，不然面试时会翻车**

---

### 进阶学习建议

如果你想加深理解，可以：

1. **运行一次 lint** - 看看你的项目有没有警告/错误
   ```bash
   npm run lint
   ```

2. **故意写个错误代码** - 看 ESLint 怎么报错
   ```tsx
   // 尝试这些，看 ESLint 报什么错
   const unused = 123  // 未使用的变量
   if (true) { const [a, setA] = useState(0) }  // 条件中调用 Hook
   ```

3. **看看配置文件** - 理解每个插件的作用
   ```bash
   code eslint.config.js
   ```

---

### 总结

| 要点 | 内容 |
|-----|------|
| **最低要求** | 知道 ESLint 是什么、为什么用、怎么用 |
| **你的项目** | Vite 自带配置，了解四个插件的作用 | 
| **简历写法** | 具体说明用了什么插件，解决了什么问题 |
| **风险提示** | 写了就要能回答，不然比不写更糟 |

> **核心建议**：ESLint 本身不难，关键是理解"为什么用它"和"它解决了什么问题"。能讲清楚这两点，面试就够用了。

---

# Prettier 配置指导

## 你的疑问

> 我应该如何配置 Prettier？（只进行指导）

---

## 我的回答

### 配置步骤概览

```
完整流程：
├── 第一步：安装依赖
├── 第二步：创建配置文件
├── 第三步：添加 npm 脚本
├── 第四步：配置 VS Code（推荐）
└── 第五步：格式化现有代码
```

---

### 第一步：安装依赖

在项目根目录执行：

```bash
npm install -D prettier eslint-config-prettier
```

| 依赖 | 作用 |
|-----|------|
| `prettier` | 核心格式化工具 |
| `eslint-config-prettier` | 关闭 ESLint 中与 Prettier 冲突的规则 |

**为什么需要 `eslint-config-prettier`？**

ESLint 和 Prettier 有部分规则重叠（如缩进、引号）。如果不加这个包，两者可能冲突，导致"ESLint 报错但 Prettier 认为正确"的情况。

---

### 第二步：创建配置文件

在项目根目录创建 `.prettierrc` 文件：

```json
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100,
  "bracketSpacing": true,
  "arrowParens": "avoid"
}
```

#### 配置项说明

| 配置项 | 值 | 效果 |
|-------|-----|------|
| `semi` | `false` | 不加分号 |
| `singleQuote` | `true` | 使用单引号 |
| `tabWidth` | `2` | 缩进 2 空格 |
| `trailingComma` | `"es5"` | ES5 支持的地方加尾逗号（对象、数组） |
| `printWidth` | `100` | 每行最大 100 字符 |
| `bracketSpacing` | `true` | 对象花括号内加空格 `{ foo: 1 }` |
| `arrowParens` | `"avoid"` | 单参数箭头函数不加括号 `x => x` |

#### 你可以根据偏好调整

```json
// 如果你喜欢加分号
{ "semi": true }

// 如果你喜欢双引号
{ "singleQuote": false }

// 如果你喜欢单参数也加括号
{ "arrowParens": "always" }
```

**建议**：团队项目统一配置，个人项目随心所欲。

---

### 第三步：添加 npm 脚本

在 `package.json` 的 `scripts` 中添加：

```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "format": "prettier --write \"src/**/*.{ts,tsx,css,json}\"",
    "format:check": "prettier --check \"src/**/*.{ts,tsx,css,json}\""
  }
}
```

#### 脚本说明

| 脚本 | 命令 | 用途 |
|-----|------|------|
| `format` | 格式化并写入文件 | 本地开发时使用 |
| `format:check` | 只检查不修改 | CI 中检查格式是否一致 |

#### 运行方式

```bash
# 格式化所有代码
npm run format

# 检查格式（CI 用）
npm run format:check
```

---

### 第四步：配置 VS Code（强烈推荐）

#### 4.1 安装插件

在 VS Code 扩展商店搜索并安装 **Prettier - Code formatter**

#### 4.2 配置保存时自动格式化

在项目根目录创建 `.vscode/settings.json`：

```json
{
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit"
  }
}
```

#### 效果

- 保存文件时自动格式化
- 保存时自动修复 ESLint 可修复的问题

---

### 第五步：格式化现有代码

配置完成后，运行一次格式化所有现有代码：

```bash
npm run format
```

这会格式化 `src/` 下所有 `.ts`、`.tsx`、`.css`、`.json` 文件。

---

### 完整文件清单

配置完成后，你会新增/修改这些文件：

```
项目根目录/
├── .prettierrc ─────────────── 新增：Prettier 配置
├── .vscode/
│   └── settings.json ────────── 新增：VS Code 配置（可选）
└── package.json ─────────────── 修改：添加依赖和脚本
```

---

### 可选：添加忽略文件

如果有些文件不想格式化，创建 `.prettierignore`：

```
dist
node_modules
*.min.js
```

---

### 验证配置是否生效

#### 方法 1：手动运行

```bash
npm run format
```

看到文件列表输出说明成功。

#### 方法 2：故意写乱格式

```tsx
// 故意写成这样
const a    =     {    foo:   1  }
```

保存后（或运行 `npm run format`）应该自动变成：

```tsx
const a = { foo: 1 }
```

---

### 与 ESLint 配合（关键）

你的 `eslint.config.js` 需要添加 `eslint-config-prettier`：

```javascript
import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import tseslint from 'typescript-eslint'
import { defineConfig, globalIgnores } from 'eslint/config'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      js.configs.recommended,
      tseslint.configs.recommended,
      reactHooks.configs.flat.recommended,
      reactRefresh.configs.vite,
      // 添加这一行，放在最后
      'prettier',
    ],
    languageOptions: {
      ecmaVersion: 2020,
      globals: globals.browser,
    },
  },
])
```

**注意**：`'prettier'` 必须放在 `extends` 数组的最后，这样才能覆盖前面冲突的规则。

---

### 常见问题

#### Q1：Prettier 和 ESLint 格式冲突怎么办？

确保安装了 `eslint-config-prettier` 并在 ESLint 配置的 `extends` 最后添加 `'prettier'`。

#### Q2：保存时没有自动格式化？

1. 确认安装了 Prettier 插件
2. 确认 VS Code 设置了 `editor.formatOnSave: true`
3. 尝试手动运行 `npm run format` 看是否有效

#### Q3：想用不同的配置怎么办？

修改 `.prettierrc` 中的值即可，Prettier 很灵活。

---

### 配置完成后的面试话术

如果面试官问：

> "你们项目的代码格式化是怎么做的？"

**参考回答**：

> 我们用 Prettier 做代码格式化，配合 ESLint 确保代码质量。
> 
> 具体配置上，我们设置了单引号、不加分号、2 空格缩进，这些是团队约定的风格。通过 `eslint-config-prettier` 解决了 ESLint 和 Prettier 的规则冲突。
> 
> 开发时 VS Code 保存自动格式化，CI 中会用 `prettier --check` 检查格式一致性，不符合规范的代码无法合并。

---

### 总结

| 步骤 | 操作 | 时间 |
|-----|------|------|
| 1. 安装依赖 | `npm install -D prettier eslint-config-prettier` | 1 分钟 |
| 2. 创建配置 | `.prettierrc` 文件 | 2 分钟 |
| 3. 添加脚本 | `package.json` 的 `scripts` | 1 分钟 |
| 4. VS Code 配置 | `.vscode/settings.json` | 2 分钟 |
| 5. 格式化代码 | `npm run format` | 1 分钟 |
| **总计** | | **约 10 分钟** |

> **一句话**：Prettier 配置简单，10 分钟搞定。核心是配置文件 + VS Code 集成 + ESLint 兼容。

---

**更新日期**：2026-03-14