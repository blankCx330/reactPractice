# 天气咨询应用 - 简历项目描述

> 基于项目代码深度分析生成，适用于前端实习生/初级前端工程师岗位

---

## 📋 项目基本信息

| 项目名称 | 天气咨询应用 |
|---------|------------|
| 技术栈 | React 19 + TypeScript 5.9 + Vite 7 + TanStack Query |
| API服务 | 和风天气 API |
| 项目周期 | 2026.03 |
| 项目状态 | 已完成核心功能 |

---

## 🎯 推荐简历写法（按篇幅选择）

### 版本一：简洁版（适合简历空间有限）

> **天气咨询应用** | React + TypeScript + Vite
> 
> 基于 React 19 和 TypeScript 构建的天气查询应用，集成和风天气 API，实现实时天气、7日预报、空气质量查询等功能。使用 TanStack Query 管理数据请求，实现请求缓存、条件查询和自动重试；完成 460+ 行 TypeScript 类型定义，确保 API 响应类型安全；实现地理位置定位和城市搜索联想功能。

---

### 版本二：标准版（推荐）

> **天气咨询应用** | React 19 + TypeScript + Vite
> 
> - 基于 React 19 和 TypeScript 构建的天气查询单页应用，集成和风天气 API，支持实时天气、7日预报、空气质量、24小时逐时预报等核心功能
> - 使用 TanStack Query 进行数据管理，实现 `enabled` 条件查询控制、`staleTime` 缓存策略（5分钟）、`retry` 自动重试机制，优化请求性能
> - 实现 Geolocation API 地理定位功能，自动获取用户当前位置天气信息，支持城市搜索联想和热门城市快速切换
> - 编写 460+ 行 TypeScript 类型定义文件，完整覆盖 API 响应结构，继承 `QWeatherBaseResponse` 基类实现类型复用
> - 采用组件化架构设计，将应用拆分为 TopContainer（搜索）、LeftContainer（天气展示）、RightContainer（详细数据）三大模块

---

### 版本三：详细版（适合项目介绍页面或面试准备）

> **天气咨询应用** | React 19 + TypeScript 5.9 + Vite 7 + TanStack Query
> 
> **项目背景**：独立开发的天气查询单页应用，旨在实践 React 19 新特性和 TypeScript 类型编程，提升前端工程化能力。
> 
> **核心功能**：
> - 实时天气展示：温度、体感温度、风向风力、湿度、气压、能见度
> - 7日天气预报：日期、天气状况、最高/最低温度、天气图标
> - 空气质量指数：AQI数值、污染等级、首要污染物
> - 24小时逐时预报：未来24小时天气变化趋势
> - 地理定位：自动获取用户当前位置
> - 城市搜索：输入联想、热门城市推荐
> 
> **技术亮点**：
> - **数据管理**：使用 TanStack Query 替代传统 fetch + useState 方案，实现请求缓存、条件查询（`enabled`）、自动重试（`retry`）、数据新鲜度控制（`staleTime: 5min`）
> - **类型安全**：编写 460 行 TypeScript 类型定义，覆盖 10+ 个 API 响应结构，使用 `extends` 实现类型继承复用
> - **组件架构**：三层组件结构（WeatherApp → Container → Feature Components），职责清晰，props 传递规范
> - **安全实践**：API Key 通过 `.env` 环境变量管理，避免敏感信息硬编码
> - **学习能力**：维护「错题本」记录开发过程中的 14 个问题及解决方案，展现良好的学习习惯

---

## 💡 面试亮点提炼

### 1. TanStack Query 的深入使用

**简历描述要点**：
- `enabled` 条件查询控制（位置有效才发起请求）
- `staleTime` 缓存策略（5分钟内数据视为新鲜）
- `queryKey` 依赖管理（经纬度变化自动重新请求）
- `retry` 重试机制（定位失败后重试1次）

**代码示例**（面试可展示）：
```tsx
const useUserLocation = () => {
  return useQuery<UserLocation>({
    queryKey: ['userLocation'],
    queryFn: async () => {
      const position = await getLocation()
      return { lat: position.coords.latitude, lon: position.coords.longitude }
    },
    staleTime: 5 * 60 * 1000,  // 5分钟内数据被认为是新鲜的
    retry: 1                    // 失败后重试一次
  })
}
```

---

### 2. TypeScript 类型定义

**简历描述要点**：
- 完整的 API 响应类型定义（460+ 行）
- 类型继承与复用（`extends QWeatherBaseResponse`）
- 可选属性处理（高纬度地区日出日落可能为空）

**代码示例**（面试可展示）：
```tsx
// 基础响应类型
export interface QWeatherBaseResponse {
  code: string
  refer?: { sources?: string[]; license?: string[] }
}

// 继承基础类型
export interface WeatherNowResponse extends QWeatherBaseResponse {
  updateTime: string
  fxLink: string
  now: WeatherNow
}
```

---

### 3. 问题解决能力

**简历描述要点**：
- 维护「错题本」记录开发问题（14条，含代码错误6条、知识点8条）
- 展现主动学习和问题归纳能力
- 体现对 React 状态管理、API 请求、TypeScript 类型的深入理解

**典型问题示例**：
| 问题类型 | 问题描述 | 解决方案 |
|---------|---------|---------|
| 状态管理 | `setState` 后立即使用状态获取旧值 | React 状态更新是异步的，用 `useEffect` 监听或直接用变量 |
| API请求 | 经纬度默认值搞反导致 400 错误 | 北京坐标是经度116、纬度39，API格式 `location=经度,纬度` |
| React规范 | 列表渲染缺少 key 属性 | 使用数据的唯一标识（如 `fxDate`）作为 key |

---

## 📊 技术能力映射

| 技术点 | 项目体现 | 面试可深挖方向 |
|-------|---------|--------------|
| React Hooks | `useState`、`useEffect`、`useQuery` | 自定义 Hooks 封装、Hooks 依赖数组 |
| TypeScript | 类型定义、泛型、接口继承 | 类型体操、工具类型、类型守卫 |
| 数据请求 | TanStack Query、fetch | 请求封装、错误处理、取消请求 |
| 状态管理 | URL 参数 + props 传递 | Context、Zustand、状态提升 |
| 工程化 | Vite、ESLint、环境变量 | 构建优化、CI/CD、测试 |

---

## ⚠️ 简历写作注意事项

### ✅ 应该强调的
- 技术栈现代化（React 19、TypeScript 5.9、Vite 7）
- TanStack Query 的具体配置和作用
- TypeScript 类型定义的完整性
- 主动学习习惯（错题本）

### ❌ 不要过度包装
- 不要说"精通 React Query"，而是"使用 TanStack Query 进行数据管理"
- 不要说"独立完成完整项目"，而是"完成核心功能开发"
- 不要夸大功能复杂度，诚实说明是练手项目

### 🔄 可补充的改进方向
面试中可能会问到项目的不足，可以主动提到：
- 缺少错误边界和 Loading 状态
- 没有单元测试
- API 调用分散在组件中，应抽离为 service 层
- 使用了 `any` 类型，需要进一步优化

---

## 📝 投递建议

### 适合投递的岗位
- 前端实习生
- 初级前端工程师
- React 开发实习生

### 投递前建议完善
1. **必须修复**：移除 `console.log`、添加列表 `key`、消除 `any` 类型
2. **强烈建议**：添加 Loading 状态、错误提示
3. **加分项**：添加单元测试、部署到线上

---

## 🎤 面试自述模板（1分钟）

> 这是一个基于 React 19 和 TypeScript 的天气查询应用。我使用 TanStack Query 进行数据管理，通过 `enabled` 实现条件查询控制，`staleTime` 配置缓存策略，避免重复请求。我编写了 460 多行的 TypeScript 类型定义，覆盖了和风天气 API 的响应结构。项目实现了地理定位、城市搜索、实时天气、7日预报等功能。在开发过程中，我维护了一个「错题本」，记录了 14 个遇到的问题和解决方案，这帮助我更好地理解了 React 状态更新机制和 API 请求的最佳实践。